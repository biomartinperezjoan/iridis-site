[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# SPA-style routing isn't needed here — index.html and /blog/ are both
# real static paths. This just ensures a clean 404 page if you add one later.
